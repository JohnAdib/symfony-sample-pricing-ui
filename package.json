{
  "name": "symfony-sample-pricing-ui",
  "version": "1.0.0",
  "description": "According to the selected filters, the data is received from the server and displayed on this page.",
  "author": "Javad Adib",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.2",
    "cssnano": "^5.1.7",
    "postcss": "^8.4.12",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^3.1.6"
  },
  "scripts": {
    "build": "NODE_ENV=production npx tailwindcss -i ./public/style/tailwind.css -o ./public/style/style.min.css --watch --minify"
  }
}
